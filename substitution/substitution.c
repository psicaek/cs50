#include <cs50.h>
#include <stdio.h>
#include <ctype.h>
#include <stdio.h>
#include <string.h>

int main(int argc, string argv[])
{
    // 26 letter key command line
    int letters =strlen(argv[1]);
    for(int n=0; n<letters; n++)
    {
      if (argc!=2)
     {
        printf("Usage: ./substitution key\n");
        return 1;
       int letters =strlen(argv[1]);
    for(int n=0; n<letters; n++)
    {
    

    }
    }


    //error if is not 26 letters
    //erroe if its numbers
    //error if its more command line arguments
    //every leeter switches to a sygkekrimeno letter apo ascii
    // input plaintext
    //output ciphertext






